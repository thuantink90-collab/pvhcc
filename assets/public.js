(()=>{const API=(window.HCCCOTO_API||'').replace(/\/$/,'');let apps=[];const box=document.getElementById('apps'),s=document.getElementById('search');async function call(action,data={}){if(API.includes('YOUR-ACCOUNT'))throw Error('Chưa cấu hình assets/config.js');const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,...data})});const j=await r.json();if(!j.ok)throw Error(j.error||'API error');return j.data}function render(){const q=(s.value||'').toLowerCase();box.innerHTML=apps.filter(a=>(a.name+' '+a.description).toLowerCase().includes(q)).map(a=>`<article class="card"><div class="icon">${a.icon||'◫'}</div><h3>${a.name}</h3><p>${a.description||''}</p><a href="${a.publicUrl}">Truy cập →</a></article>`).join('')||'Không có ứng dụng.'}s.oninput=render;call('public.apps').then(x=>{apps=x;render()}).catch(e=>box.innerHTML=e.message)})();
